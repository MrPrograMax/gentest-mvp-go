{{/* test.go.tmpl — reference copy of the template embedded in render/render.go.
     This file is NOT read at runtime; the template is embedded as a Go string
     constant so the binary has no external file dependencies.
     Edit render/render.go when you change the template. */}}

// Code generated by testgen. DO NOT EDIT.
package {{.PackageName}}

import (
{{- range .Imports}}
	{{.}}
{{- end}}
)
{{range .Tests}}
// {{.TestFuncName}} was generated by testgen.
// TODO: дополни проверки got* в соответствии с бизнес-логикой
// (см. строки '_ = gotN' ниже — там стоят TODO-комментарии).
func {{.TestFuncName}}(t *testing.T) {
	tests := []struct {
		name string
{{- range .StructFields}}
		{{.Name}} {{.TypeStr}}
{{- end}}
	}{
{{- range .Rows}}
		{
			name: {{printf "%q" .Name}},
{{- range .Fields}}
			{{.Name}}: {{.Value}},
{{- end}}
		},
{{- end}}
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
{{- range .SetupLines}}
			{{.}}
{{- end}}
			{{.CallLine}}
{{- range .AssertLines}}
			{{.}}
{{- end}}
		})
	}
}
{{end}}
